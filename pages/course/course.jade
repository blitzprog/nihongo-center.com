if !user
	p= __("error.notLoggedIn")
else
	.page-header
		h1= __("monthNames." + month) + " " + year
	
	each student in course.students
		p
			a.ajax(href=`/student/${student.id}`)= student.profile.givenName + " " + student.profile.familyName